If you don't have permissions to run scripts on your machine, run a PS prompt as admin and then run:

	Set-ExecutionPolicy unrestricted

PS module SqlServer is not installed by default. To install it, run PS prompt as admin and then run:

	Install-Module -Name SqlServer
	
Use text comparison tool WinMerge at https://sourceforge.net/projects/winmerge/